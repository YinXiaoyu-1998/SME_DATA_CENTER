# 企业资料中枢 Test Cases

This file tracks manual and automated acceptance scenarios as implementation criteria become tests.

## Phase 1 / Day 0 - Skeleton

| Case                  | Type            | Command / Evidence                                         | Expected Result                                                      | Status |
| --------------------- | --------------- | ---------------------------------------------------------- | -------------------------------------------------------------------- | ------ |
| Install dependencies  | Automated setup | `npm install`                                              | Creates `package-lock.json` and installs workspace dependencies.     | Pass   |
| Start local MySQL     | Local smoke     | `MYSQL_PORT=3307 docker compose up -d mysql`; health check | MySQL container starts and reports healthy or running.               | Pass   |
| Lint exists and runs  | Automated smoke | `npm run lint`                                             | Lint command exits successfully.                                     | Pass   |
| Typecheck runs        | Automated smoke | `npm run typecheck`                                        | TypeScript check exits successfully.                                 | Pass   |
| Test exists and runs  | Automated smoke | `npm test`                                                 | Vitest smoke test exits successfully.                                | Pass   |
| Tracking docs exist   | Manual review   | `ls docs/implementation/*.md`                              | Required long-term progress docs are present.                        | Pass   |
| Secrets not committed | Manual review   | `git status --short` and file review                       | `.env`, secrets, and real customer data are not staged or committed. | Pass   |

## Phase 1 / Day 1A - Data Model, Migrations, And Seeds

| Case                                    | Type                 | Command / Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Expected Result                                                                                   | Status |
| --------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------ |
| Prisma schema validates                 | Automated schema     | `npx prisma validate --config packages/db/prisma.config.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Schema is valid for MySQL.                                                                        | Pass   |
| Migration creates Day 1A tables         | Local DB smoke       | `MYSQL_PORT=3307 docker compose up -d mysql`; local Prisma shadow DB grant supplied by `docker/mysql/init/001-grant-prisma-shadow.sql` for fresh volumes; existing Day 0 volume was granted once manually; `DATABASE_URL='mysql://enterprise_hub:enterprise_hub_local_password@localhost:3307/enterprise_hub' npm run db:migrate -- --name init_day_1a`; table query shows `organizations`, `employees`, `labels`, `employee_labels`, `documents`, `document_labels`, `processing_runs`, `document_chunks`, `audit_logs`, `skill_entries`, `access_tokens`. | All required Day 1A tables exist in local MySQL.                                                  | Pass   |
| Seed inserts expected local identities  | Local DB smoke       | `DATABASE_URL='mysql://enterprise_hub:enterprise_hub_local_password@localhost:3307/enterprise_hub' npm run db:seed`; `DATABASE_URL='mysql://enterprise_hub:enterprise_hub_local_password@localhost:3307/enterprise_hub' npm --workspace @enterprise-hub/db run db:seed`                                                                                                                                                                                                                                                                                     | Inserts `default-org`, four employees, seven labels, and expected employee-label assignments.     | Pass   |
| Seed is idempotent                      | Local DB smoke       | Run root and workspace seed commands multiple times with the same `DATABASE_URL`; output remains `organizations=1, employees=4, labels=7, employee_labels=12`; DB query shows `personal_labels=4`.                                                                                                                                                                                                                                                                                                                                                          | No duplicate orgs, employees, labels, personal labels, or employee-label assignments are created. | Pass   |
| Permission helper covers required cases | Automated unit tests | `npm test -- packages/domain/src/permissions.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Store match, personal match, all-staff visibility, no match false, disabled false all pass.       | Pass   |
| Duplicate constraints exist             | Local DB inspection  | Information schema query for indexes shows `employees_email_key(email)`, `labels_org_id_key_key(org_id,key)`, and `document_labels` primary key `(document_id,label_id)`.                                                                                                                                                                                                                                                                                                                                                                                   | Duplicate employee email, label key per org, and document-label pair are prevented by the DB.     | Pass   |
| Repository verification remains green   | Automated checks     | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Tests, TypeScript, ESLint, and Prettier checks exit successfully.                                 | Pass   |

## Phase 1 / Day 1B - API App Shell And Auth Context

| Case                                  | Type             | Command / Evidence                                                                                                                                                                                                                                                                                                                            | Expected Result                                                                                             | Status |
| ------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------ |
| Health endpoint                       | Automated test   | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                                                                                                                     | `GET /healthz` returns HTTP 200 with `{ "ok": true, "service": "enterprise-hub-api" }`.                     | Pass   |
| Dev login returns token               | Automated test   | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                                                                                                                     | `POST /auth/dev-login` for `admin@example.com` returns `accessToken` and employee fields without passwords. | Pass   |
| Authenticated `/me` returns labels    | Automated test   | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                                                                                                                     | `GET /me` with a bearer token returns employee id, email, role, disabled flag, and label keys.              | Pass   |
| Missing or invalid token rejected     | Automated test   | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                                                                                                                     | `GET /me` without a bearer token or with an invalid token returns HTTP 401 and `UNAUTHENTICATED`.           | Pass   |
| Disabled employee rejected            | Automated test   | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                                                                                                                     | A valid token for a disabled employee returns HTTP 403 and `EMPLOYEE_DISABLED`.                             | Pass   |
| Repository verification remains green | Automated checks | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`                                                                                                                                                                                                                                                                       | Tests, TypeScript, ESLint, and Prettier checks exit successfully.                                           | Pass   |
| Local API curl smoke                  | Local smoke      | `MYSQL_PORT=3307 docker compose up -d mysql`; `npm run db:generate`; `DATABASE_URL='mysql://enterprise_hub:enterprise_hub_local_password@localhost:3307/enterprise_hub' npm run db:seed`; start `npm run api:dev`; curl `/healthz`, `/me` without a token, `/me` with an invalid token, `/auth/dev-login`, and `/me` with the returned token. | Running API uses Day 1A seeded employee data and returns the documented JSON responses.                     | Pass   |

## Phase 1 / P0 Correction - Label Catalog

| Case                                  | Type               | Command / Evidence                                                      | Expected Result                                                                                                      | Status |
| ------------------------------------- | ------------------ | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------ |
| Label catalog red tests               | TDD evidence       | `npm test -- apps/api/src/server.test.ts`                               | Two `GET /labels` tests failed with HTTP 404 before the P0 correction route was implemented.                         | Pass   |
| Auth required for label catalog       | Automated API test | `npm test -- apps/api/src/server.test.ts`                               | `GET /labels` without a bearer token returns HTTP 401 `UNAUTHENTICATED`.                                             | Pass   |
| Authenticated label catalog response  | Automated API test | `npm test -- apps/api/src/server.test.ts`                               | Authenticated `GET /labels` returns label `key`, `name`, and `type`, ordered by type then key.                       | Pass   |
| Internal label ids are not exposed    | Automated API test | `npm test -- apps/api/src/server.test.ts`                               | The `GET /labels` response does not include internal label ids such as `label_store_baoli`.                          | Pass   |
| Repository verification remains green | Automated checks   | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check` | Tests, TypeScript, ESLint, and Prettier checks exit successfully after adding the label catalog correction endpoint. | Pass   |

## Phase 1 / Day 1C - Storage Adapter

| Case                                  | Type                | Command / Evidence                                                      | Expected Result                                                                                                                                                                                    | Status |
| ------------------------------------- | ------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Storage adapter red test              | TDD evidence        | `npm test -- packages/storage/src/local-storage.test.ts`                | Initial test run failed because `./local-storage.js` was missing before implementation.                                                                                                            | Pass   |
| Local original file object round trip | Automated unit test | `npm test -- packages/storage/src/local-storage.test.ts`                | Local adapter writes exact bytes, returns deterministic `org/<orgId>/documents/<documentId>/original/<safeFileName>` key, stats size/type/hash, reads bytes, and returns a `file://` download URL. | Pass   |
| Repository verification remains green | Automated checks    | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check` | Tests, TypeScript, ESLint, and Prettier checks exit successfully after adding the storage adapter.                                                                                                 | Pass   |

## Phase 1 / Day 2 - Document Upload And Catalog

| Case                                   | Type               | Command / Evidence                                                                                                                                                               | Expected Result                                                                                                                                                                       | Status |
| -------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Upload route red tests                 | TDD evidence       | `npm test -- apps/api/src/server.test.ts` before implementation                                                                                                                  | Four Day 2 tests failed with HTTP 404 because `/documents` and `/documents/:id/status` were not implemented yet.                                                                      | Pass   |
| Multipart upload catalogs pending doc  | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Authenticated upload returns HTTP 201, `pending_processing`, storage object key, `store:baoli`, uploader personal label, queued run status, audit evidence, and matching file bytes.  | Pass   |
| Unknown label rejected                 | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Upload with unknown label returns HTTP 400 `UNKNOWN_LABEL` and creates no document or storage file.                                                                                   | Pass   |
| Forbidden existing label rejected      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Non-admin upload with another employee/store label returns HTTP 403 `FORBIDDEN_LABEL` and creates no document or storage file.                                                        | Pass   |
| Non-admin all-staff label rejected     | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Non-admin upload requesting `all_staff` returns HTTP 403 `FORBIDDEN_LABEL`.                                                                                                           | Pass   |
| Upload without requested labels        | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Upload succeeds with the uploader personal label as the final label set.                                                                                                              | Pass   |
| Catalog failure cleans storage object  | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | If the storage write succeeds but catalog persistence fails, the API returns HTTP 500 and removes the temporary stored object.                                                        | Pass   |
| Status visibility                      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                        | Uploader and admin can read status; another non-admin employee receives HTTP 404.                                                                                                     | Pass   |
| Real local DB and storage upload smoke | Local integration  | `MYSQL_PORT=3307 docker compose up -d mysql`; `DATABASE_URL=... npm run db:generate`; `DATABASE_URL=... npm run db:seed`; injected API smoke with temporary `LOCAL_STORAGE_ROOT` | Created one `documents` row, two `document_labels`, one queued `processing_runs` row, one `document.uploaded` audit row, and stored bytes matching `fixtures/baoli-june-meituan.csv`. | Pass   |
| Repository verification remains green  | Automated checks   | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`                                                                                                          | Tests, TypeScript, ESLint, and Prettier checks exit successfully after adding upload/catalog.                                                                                         | Pass   |

## Phase 1 / Day 3 - Processing Worker And Active Visibility

| Case                                           | Type                | Command / Evidence                                                                                                                                                                                                                                | Expected Result                                                                                                                                                                  | Status |
| ---------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Worker red tests                               | TDD evidence        | `npm test -- apps/worker/src/worker.test.ts` before implementation                                                                                                                                                                                | Failed because `./worker.js` was missing before the Day 3 worker implementation.                                                                                                 | Pass   |
| Supported CSV activates and writes chunks      | Automated unit test | `npm test -- apps/worker/src/worker.test.ts`                                                                                                                                                                                                      | A queued CSV document is claimed, marked processing, read from local storage, chunked, written to `document_chunks`, marked `active`, and audited with processing/active events. | Pass   |
| Worker rerun is idempotent for chunks          | Automated unit test | `npm test -- apps/worker/src/worker.test.ts`                                                                                                                                                                                                      | Running worker processing twice for the same document does not duplicate chunks because writes dedupe on `(document_id, chunk_hash, index_type)`.                                | Pass   |
| Unsupported file follows bounded retry failure | Automated unit test | `npm test -- apps/worker/src/worker.test.ts`                                                                                                                                                                                                      | Unsupported `.pdf` input records `UNSUPPORTED_FILE_TYPE`, retries through the bounded path, and becomes `processing_failed` after three failures.                                | Pass   |
| Failed status visibility                       | Automated API test  | `npm test -- apps/api/src/server.test.ts`                                                                                                                                                                                                         | A `processing_failed` document status is visible to the uploader and admin, and returns HTTP 404 for another ordinary employee. Ordinary search remains Day 4A scope.            | Pass   |
| Real local worker smoke                        | Local integration   | `MYSQL_PORT=3307 docker compose up -d mysql`; `DATABASE_URL=... npm run db:generate`; `DATABASE_URL=... npm run db:seed`; upload a synthetic CSV through the API; `DATABASE_URL=... LOCAL_STORAGE_ROOT=... npm run worker:once`; inspect DB rows. | Uploaded CSV moves from `pending_processing` to `active`; `document_chunks` has at least one row; a second `worker:once` does not add duplicate chunks.                          | Pass   |
| Repository verification remains green          | Automated checks    | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`                                                                                                                                                                           | Tests, TypeScript, ESLint, and Prettier checks exit successfully after adding the worker.                                                                                        | Pass   |

## Phase 1 / Day 4A - Permission-Filtered Search And Download

| Case                                     | Type               | Command / Evidence                                                                                                                                                             | Expected Result                                                                                                                                                                            | Status |
| ---------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Document query red tests                 | TDD evidence       | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | Four Day 4A tests failed with HTTP 404 because `/documents`, `/documents/:id`, and `/documents/:id/download` were not implemented yet.                                                     | Pass   |
| Baoli manager sees active Baoli document | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | `GET /documents?q=Meituan` as Baoli manager returns the active Baoli document and excludes pending, archived, and inaccessible Suzhou documents.                                           | Pass   |
| Inaccessible documents do not leak       | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | Suzhou manager querying Baoli text receives an empty list; `GET /documents/:id` for the Baoli document returns `404` without title or existence leak.                                      | Pass   |
| Admin can list all active documents      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | Admin `GET /documents` returns all active documents sorted by newest `sourceTime`, then `createdAt`.                                                                                       | Pass   |
| Accessible detail and download           | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | Accessible active document detail returns metadata, and `/download` returns a local `file://` URL.                                                                                         | Pass   |
| Query and download audit events          | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                      | Query audit records returned result count without inaccessible titles; download audit records the successful target document id.                                                           | Pass   |
| Real local query/download smoke          | Local integration  | `MYSQL_PORT=3307 docker compose up -d mysql`; `npm run db:generate`; `npm run db:seed`; temporary `LOCAL_STORAGE_ROOT`; API upload; `runWorkerOnce`; API query/detail/download | Uploaded CSV becomes `active`; Baoli manager and admin can list it; Suzhou manager gets no result; detail returns metadata; download returns `file://`; audit logs include query/download. | Pass   |
| Repository verification remains green    | Automated checks   | `npm test -- apps/api/src/server.test.ts`; `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`                                                             | Tests, TypeScript, ESLint, and Prettier checks exit successfully after adding search/detail/download.                                                                                      | Pass   |

## Phase 1 / Day 4B - Skill Directory

| Case                                  | Type               | Command / Evidence                                                                                                                     | Expected Result                                                                                                                  | Status |
| ------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Skill directory red tests             | TDD evidence       | `npm test -- apps/api/src/server.test.ts`                                                                                              | Three Day 4B tests failed with HTTP 404 because `/skills` was not implemented yet.                                               | Pass   |
| Auth required for Skill Directory     | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                              | `GET /skills` without a bearer token returns HTTP 401 `UNAUTHENTICATED`.                                                         | Pass   |
| Menu skill returned for `q=菜单`      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                              | `GET /skills?q=菜单` returns `menu-gross-margin-analysis` with install instructions and example prompts.                         | Pass   |
| Skill Directory does not execute      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                              | `/skills` response contains metadata and does not contain execution result fields.                                               | Pass   |
| Disabled skills are not returned      | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                              | Disabled test skill does not appear in search or category-filtered responses.                                                    | Pass   |
| Category filter returns approved item | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                              | `GET /skills?category=reporting` returns only `weekly-store-report`.                                                             | Pass   |
| Real local Skill Directory smoke      | Local integration  | `MYSQL_PORT=3307 docker compose up -d mysql`; `npm run db:generate`; `npm run db:seed` twice; injected API smoke                       | Seed remains idempotent at two skill entries; authenticated API query returns the menu skill and category filter returns weekly. | Pass   |
| Repository verification remains green | Automated checks   | `npm test -- apps/api/src/server.test.ts`; `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`; `git diff --check` | Tests, TypeScript, ESLint, Prettier, and diff checks exit successfully after adding Skill Directory.                             | Pass   |

## Phase 1 / Day 4C - Audit, Archive, And Label Sharing

| Case                                             | Type               | Command / Evidence                                                                                                                                                                      | Expected Result                                                                                                                                                 | Status |
| ------------------------------------------------ | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Archive/share/audit red tests                    | TDD evidence       | `npm test -- apps/api/src/server.test.ts` before implementation                                                                                                                         | Five Day 4C tests failed with HTTP 404 because `/documents/:id/archive`, `/documents/:id/labels`, and `/audit` were not implemented yet.                        | Pass   |
| Archive hides active document                    | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                               | Uploader archives an active document, response status becomes `archived`, ordinary search no longer returns it, and the original local storage bytes remain.    | Pass   |
| Existing personal label sharing                  | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                               | Uploader adds existing `person:lijie`; `lijie@example.com` can then find the active document through ordinary search.                                           | Pass   |
| Unknown label rejected                           | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                               | Label mutation with a missing catalog label returns HTTP 400 `UNKNOWN_LABEL` and leaves document labels unchanged.                                              | Pass   |
| Non-uploader non-admin label mutation is blocked | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                               | A non-uploader non-admin receives HTTP 403 `DOCUMENT_UPDATE_FORBIDDEN` for an otherwise visible document and cannot change labels.                              | Pass   |
| Admin-only ordered audit query                   | Automated API test | `npm test -- apps/api/src/server.test.ts`                                                                                                                                               | Admin receives ordered audit events from newest to oldest; non-admin receives HTTP 403 `FORBIDDEN`.                                                             | Pass   |
| Real local archive/share/audit smoke             | Local integration  | `MYSQL_PORT=3307 docker compose up -d mysql`; `npm run db:generate`; `npm run db:seed`; temporary `LOCAL_STORAGE_ROOT`; API upload; `runWorkerOnce`; API label/share/archive/audit flow | Seed includes `lijie@example.com`; Li Jie sees a shared document, archive removes it from search without deleting storage, and admin audit query shows changes. | Pass   |
| Repository verification remains green            | Automated checks   | `npm test -- apps/api/src/server.test.ts`; `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`; `git diff --check`                                                  | Tests, TypeScript, ESLint, Prettier, and diff checks exit successfully after adding archive/share/audit APIs.                                                   | Pass   |

## Phase 1 / Day 5 - Minimal CLI Smoke Interface

| Case                                   | Type              | Command / Evidence                                                                                                                                                                  | Expected Result                                                                                                                                           | Status |
| -------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| CLI login stores token locally         | Automated unit    | `npm test -- apps/cli/src/cli.test.ts`                                                                                                                                              | `hub login --email baoli.manager@example.com` calls `/auth/dev-login`, stores the token under the ignored session file, and does not print the token.     | Pass   |
| CLI search calls API with bearer token | Automated unit    | `npm test -- apps/cli/src/cli.test.ts`                                                                                                                                              | `hub documents search "保利店 美团"` calls `GET /documents` with the stored bearer token and returns only the API response.                               | Pass   |
| CLI upload sends multipart API request | Automated unit    | `npm test -- apps/cli/src/cli.test.ts`                                                                                                                                              | `hub documents upload ./fixtures/baoli-june-meituan.csv --label store:baoli` sends multipart `POST /documents` using the stored bearer token.             | Pass   |
| Real local CLI smoke                   | Local integration | `MYSQL_PORT=3307 docker compose up -d mysql`; `npm run db:generate`; `npm run db:seed`; start API on a local port; `npm run hub -- login`; `npm run hub -- documents upload/search` | A developer can login, upload a fixture, run the worker, search it as Baoli manager, and confirm Suzhou manager CLI search cannot see the Baoli document. | Pass   |
| Repository verification remains green  | Automated checks  | `npm test -- apps/cli/src/cli.test.ts`; `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`; `git diff --check`                                                 | Tests, TypeScript, ESLint, Prettier, and diff checks exit successfully after adding the CLI smoke interface.                                              | Pass   |

## Phase 1 / Day 6 - MVP Integration Test And Local Demo

| Case                                  | Type              | Command / Evidence                                                                                                      | Expected Result                                                                                                                                                                                                                                                          | Status |
| ------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Fixtures are non-sensitive            | Manual review     | Review `fixtures/baoli-june-meituan.csv`, `fixtures/suzhou-performance.md`, and `fixtures/management-knowledge.md`      | Fixture files use synthetic sample values only and do not contain secrets, real customer exports, or private company data.                                                                                                                                               | Pass   |
| Clean local MVP integration loop      | Local integration | `MYSQL_PORT=3307 npm run test:integration`                                                                              | Script starts local MySQL, resets and seeds the local development DB, starts the API, logs in as Baoli, uploads the Baoli fixture, runs worker once, finds it as Baoli, hides it from Suzhou, downloads it as Baoli, archives it, and confirms ordinary search hides it. | Pass   |
| Repository verification remains green | Automated checks  | `npm test`; `npm run test:integration`; `npm run typecheck`; `npm run lint`; `npm run format:check`; `git diff --check` | Tests, integration script, TypeScript, ESLint, Prettier, and diff checks exit successfully after adding the Day 6 local demo.                                                                                                                                            | Pass   |

## Phase 2 / Day 1 - MCP Server Skeleton And Tool Contract

| Case                                      | Type                | Command / Evidence                                                                          | Expected Result                                                                                                  | Status |
| ----------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------ |
| MCP skeleton red tests                    | TDD evidence        | `npm test -- apps/mcp-server/src/config.test.ts apps/mcp-server/src/tools.test.ts`          | Initial test run failed because `config.ts` and `tools.ts` did not exist before Day 1 implementation.            | Pass   |
| MCP runtime config validates local URL    | Automated unit test | `npm test -- apps/mcp-server/src/config.test.ts apps/mcp-server/src/tools.test.ts`          | Missing `ENTERPRISE_HUB_API_URL` and invalid URL values fail with clear messages; valid URL/profile normalizes.  | Pass   |
| MCP tool contract is deterministic        | Automated unit test | `npm test -- apps/mcp-server/src/config.test.ts apps/mcp-server/src/tools.test.ts`          | The required nine Phase 2 tool names, descriptions, input fields, and result shapes are defined in stable order. | Pass   |
| Missing MCP API URL fails startup clearly | Local smoke         | `npm run mcp:dev`                                                                           | Command exits non-zero and prints that `ENTERPRISE_HUB_API_URL` is required.                                     | Pass   |
| MCP stdio server starts with local URL    | Local smoke         | `ENTERPRISE_HUB_API_URL=http://127.0.0.1:3000 npm run mcp:dev </dev/null`                   | Command starts the stdio MCP server without starting API, worker, MySQL, or storage services.                    | Pass   |
| Repository verification remains green     | Automated checks    | `npm test`; `npm run typecheck`; `npm run lint`; `npm run format:check`; `git diff --check` | Tests, TypeScript, ESLint, Prettier, and diff checks exit successfully after adding the MCP skeleton.            | Pass   |
